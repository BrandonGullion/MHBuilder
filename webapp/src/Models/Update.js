class Update {
    constructor(id, updateTitle, updateContent, date) {
        this.id = id;
        this.updateTitle = updateTitle;
        this.updateContent = updateContent;
        this.date = date;
    }
}

export default Update;
